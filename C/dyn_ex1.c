#include <stdio.h>
#include <stdlib.h>

int main() {
	system("clear");
	
	int *p = malloc(sizeof(int));		//malloc return void * and we are using int *; we need to cast void * to int *
		
	return 0;
}
